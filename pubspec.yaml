name: interiorapp_flutter_client
description: "interiorapp client"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  # 언어 설정
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8
  
  # 라우팅
  go_router: ^14.8.1
  # 상태관리
  flutter_riverpod: ^3.0.0
  # 반응형 레이아웃
  flutter_screenutil: ^5.9.3
  # 네트워크
  dio: ^5.8.0+1
  # 캐싱
  shared_preferences: ^2.5.3
  cached_network_image: ^3.4.1
  # 환경변수 파일
  flutter_dotenv: ^5.2.1
  # intl
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

  # lint
  custom_lint: ^0.8.0
  riverpod_lint: ^3.0.0

flutter:
  uses-material-design: true

  # assets
  # assets:
    # - assets/images/
