import 'package:flutter/material.dart';

class ConstructScreen extends StatelessWidget {
  const ConstructScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('ConstructScreen')));
  }
}
