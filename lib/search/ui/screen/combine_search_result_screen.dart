import 'package:flutter/material.dart';

class CombineSearchResultScreen extends StatelessWidget {
  const CombineSearchResultScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('CombineSearchResultScreen')));
  }
}
